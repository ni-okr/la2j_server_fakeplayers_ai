# 🎮 Технические требования для разработки Modern Lineage II Client

## 📋 Обзор

**Дата создания**: 28 сентября 2024  
**Версия**: v5.0 → v6.0  
**Статус**: Активные требования  
**Цель**: Обеспечение качества и единообразия разработки

---

## 🌍 ЯЗЫКОВЫЕ ТРЕБОВАНИЯ

### 🚨 КРИТИЧЕСКИ ВАЖНО

**ВСЕ комментарии к коду, технические пояснения, коммиты и документация ДОЛЖНЫ быть на русском языке!**

#### ✅ Правильно:
```cpp
// Создание системы рельефа для игрового мира
class TerrainSystem {
    // Генерация высот рельефа с использованием шума Перлина
    void generateTerrainHeights();
    
    // Применение текстуры земли на основе высоты
    void applyGroundTexture(float height);
};
```

#### ❌ Неправильно:
```cpp
// Creating terrain system for game world
class TerrainSystem {
    // Generate terrain heights using Perlin noise
    void generateTerrainHeights();
    
    // Apply ground texture based on height
    void applyGroundTexture(float height);
};
```

### 📝 Требования к комментариям

#### 1. **Заголовки функций**
```cpp
/**
 * Создает систему неба с различными типами погоды
 * @param skyType - тип неба (день, ночь, облачно, дождь)
 * @param weatherIntensity - интенсивность погоды (0.0-1.0)
 * @return true если небо успешно создано
 */
bool createSkySystem(SkyType skyType, float weatherIntensity);
```

#### 2. **Комментарии к переменным**
```cpp
// Время суток: 0.0 = полночь, 0.5 = полдень, 1.0 = полночь
float timeOfDay = 0.5f;

// Покрытие облаками: 0.0 = ясно, 1.0 = полностью облачно
float cloudCover = 0.3f;

// Интенсивность дождя: 0.0 = нет дождя, 1.0 = сильный дождь
float rainIntensity = 0.0f;
```

#### 3. **Комментарии к сложной логике**
```cpp
// Смешивание текстур на основе высоты рельефа
// Если высота < 0.3 - трава, 0.3-0.7 - земля, > 0.7 - камень
if (height < 0.3f) {
    texture = grassTexture;
} else if (height < 0.7f) {
    texture = groundTexture;
} else {
    texture = stoneTexture;
}
```

---

## 🎯 АРХИТЕКТУРНЫЕ ТРЕБОВАНИЯ

### 🏗️ Структура проекта

#### 1. **Организация файлов**
```
modern_client/
├── Source/ModernLineage2/          # Исходный код C++
│   ├── Public/                     # Заголовочные файлы
│   │   ├── World/                  # Системы мира
│   │   │   ├── TerrainSystem.h     # Система рельефа
│   │   │   ├── SkySystem.h         # Система неба
│   │   │   ├── WaterSystem.h       # Система воды
│   │   │   └── VegetationSystem.h  # Система растительности
│   │   ├── Characters/             # Системы персонажей
│   │   └── UI/                     # Интерфейсы
│   └── Private/                    # Реализация
├── Content/                        # Игровой контент
│   ├── Textures/                   # Текстуры
│   ├── Meshes/                     # 3D модели
│   ├── Materials/                  # PBR материалы
│   └── UI/                         # Интерфейсы
└── Config/                         # Конфигурации
```

#### 2. **Именование классов**
```cpp
// Системы мира
class ATerrainSystem : public AActor
class ASkySystem : public AActor
class AWaterSystem : public AActor

// Компоненты
class UTerrainComponent : public USceneComponent
class USkyComponent : public USceneComponent
class UWaterComponent : public USceneComponent

// Менеджеры
class UWorldManager : public UObject
class UTextureManager : public UObject
class UMaterialManager : public UObject
```

#### 3. **Именование переменных**
```cpp
// Система рельефа
int32 TerrainSize = 100;
float TerrainScale = 2.0f;
TArray<TArray<float>> TerrainHeights;

// Система неба
float TimeOfDay = 0.5f;
bool bDayNightCycle = true;
float CloudCover = 0.3f;
bool bIsRaining = false;

// Система воды
float WaterLevel = 0.0f;
float WaveAmplitude = 1.0f;
float WaveFrequency = 0.1f;
```

---

## 🎨 ГРАФИЧЕСКИЕ ТРЕБОВАНИЯ

### 🌍 Система мира

#### 1. **Система рельефа**
```cpp
/**
 * Система рельефа для создания игрового мира
 * Поддерживает различные типы поверхности и высот
 */
class ATerrainSystem : public AActor
{
public:
    // Размер рельефа в квадах
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Рельеф")
    int32 TerrainSize = 100;
    
    // Масштаб рельефа
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Рельеф")
    float TerrainScale = 2.0f;
    
    // Высоты рельефа
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Рельеф")
    TArray<TArray<float>> TerrainHeights;
    
    // Текстуры для разных типов поверхности
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Текстуры")
    UTexture2D* GrassTexture;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Текстуры")
    UTexture2D* GroundTexture;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Текстуры")
    UTexture2D* StoneTexture;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Текстуры")
    UTexture2D* SandTexture;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Текстуры")
    UTexture2D* SnowTexture;
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Рельеф")
    void GenerateTerrain();
    
    UFUNCTION(BlueprintCallable, Category = "Рельеф")
    void LoadTerrainFromFile(const FString& FilePath);
    
    UFUNCTION(BlueprintCallable, Category = "Рельеф")
    float GetHeightAtPosition(float X, float Z) const;
    
    UFUNCTION(BlueprintCallable, Category = "Рельеф")
    UTexture2D* GetTextureAtPosition(float X, float Z) const;
};
```

#### 2. **Система неба**
```cpp
/**
 * Система неба с различными типами погоды и временем суток
 * Поддерживает дневное, ночное, облачное и дождливое небо
 */
class ASkySystem : public AActor
{
public:
    // Типы неба
    UENUM(BlueprintType)
    enum class ESkyType : uint8
    {
        Дневное        UMETA(DisplayName = "Дневное небо"),
        Облачное       UMETA(DisplayName = "Облачное небо"),
        Дождливое      UMETA(DisplayName = "Дождливое небо"),
        Ночное         UMETA(DisplayName = "Ночное небо"),
        НочноеСЛуной   UMETA(DisplayName = "Ночное небо с луной"),
        Звездное       UMETA(DisplayName = "Звездное небо")
    };
    
    // Текущий тип неба
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Небо")
    ESkyType CurrentSkyType = ESkyType::Дневное;
    
    // Время суток (0.0 = полночь, 0.5 = полдень, 1.0 = полночь)
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Небо")
    float TimeOfDay = 0.5f;
    
    // Покрытие облаками (0.0 = ясно, 1.0 = полностью облачно)
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Небо")
    float CloudCover = 0.3f;
    
    // Интенсивность дождя (0.0 = нет дождя, 1.0 = сильный дождь)
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Небо")
    float RainIntensity = 0.0f;
    
    // Цвета неба для разных типов
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Цвета")
    FLinearColor DaySkyColor = FLinearColor(0.5f, 0.7f, 1.0f, 1.0f);
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Цвета")
    FLinearColor NightSkyColor = FLinearColor(0.1f, 0.1f, 0.3f, 1.0f);
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Цвета")
    FLinearColor RainSkyColor = FLinearColor(0.4f, 0.4f, 0.5f, 1.0f);
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Небо")
    void SetSkyType(ESkyType NewSkyType);
    
    UFUNCTION(BlueprintCallable, Category = "Небо")
    void SetTimeOfDay(float NewTimeOfDay);
    
    UFUNCTION(BlueprintCallable, Category = "Небо")
    void SetWeather(float CloudCover, float RainIntensity);
    
    UFUNCTION(BlueprintCallable, Category = "Небо")
    void UpdateSky();
};
```

#### 3. **Система воды**
```cpp
/**
 * Система воды с различными типами и физикой
 * Поддерживает речную, морскую, дождевую воду и лужи
 */
class AWaterSystem : public AActor
{
public:
    // Типы воды
    UENUM(BlueprintType)
    enum class EWaterType : uint8
    {
        Речная    UMETA(DisplayName = "Речная вода"),
        Морская   UMETA(DisplayName = "Морская вода"),
        Дождевая  UMETA(DisplayName = "Дождевая вода"),
        Лужа      UMETA(DisplayName = "Лужа")
    };
    
    // Текущий тип воды
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Вода")
    EWaterType CurrentWaterType = EWaterType::Речная;
    
    // Уровень воды
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Вода")
    float WaterLevel = 0.0f;
    
    // Параметры волн
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Волны")
    float WaveAmplitude = 1.0f;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Волны")
    float WaveFrequency = 0.1f;
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Волны")
    float WaveSpeed = 1.0f;
    
    // Прозрачность воды
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Вода")
    float WaterTransparency = 0.8f;
    
    // Цвет воды
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Вода")
    FLinearColor WaterColor = FLinearColor(0.2f, 0.6f, 0.8f, 0.8f);
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Вода")
    void SetWaterType(EWaterType NewWaterType);
    
    UFUNCTION(BlueprintCallable, Category = "Вода")
    void UpdateWaves(float DeltaTime);
    
    UFUNCTION(BlueprintCallable, Category = "Вода")
    void RenderReflections();
    
    UFUNCTION(BlueprintCallable, Category = "Вода")
    void RenderUnderwater();
};
```

---

## 🎨 ТЕКСТУРНЫЕ ТРЕБОВАНИЯ

### 📁 Организация текстур

#### 1. **Структура папок**
```
Content/Textures/
├── World/                          # Текстуры мира
│   ├── Terrain/                    # Текстуры рельефа
│   │   ├── Grass/                  # Трава
│   │   ├── Ground/                 # Земля
│   │   ├── Stone/                  # Камень
│   │   ├── Sand/                   # Песок
│   │   └── Snow/                   # Снег
│   ├── Sky/                        # Текстуры неба
│   │   ├── Clouds/                 # Облака
│   │   ├── Stars/                  # Звезды
│   │   └── Moon/                   # Луна
│   └── Water/                      # Текстуры воды
│       ├── River/                  # Речная вода
│       ├── Ocean/                  # Морская вода
│       └── Rain/                   # Дождевая вода
├── Characters/                     # Текстуры персонажей
├── Buildings/                      # Текстуры зданий
└── UI/                            # Текстуры интерфейса
```

#### 2. **Именование текстур**
```cpp
// Текстуры рельефа
T_Grass_01_Diffuse      // Диффузная карта травы
T_Grass_01_Normal       // Нормальная карта травы
T_Grass_01_Roughness    // Карта шероховатости
T_Grass_01_Metallic     // Металлическая карта
T_Grass_01_AO           // Карта затенения
T_Grass_01_Emissive     // Эмиссивная карта

// Текстуры неба
T_Sky_Day_Diffuse       // Дневное небо
T_Sky_Night_Diffuse     // Ночное небо
T_Clouds_01_Diffuse     // Облака
T_Stars_01_Diffuse      // Звезды
T_Moon_01_Diffuse       // Луна

// Текстуры воды
T_Water_River_Diffuse   // Речная вода
T_Water_Ocean_Diffuse   // Морская вода
T_Water_Rain_Diffuse    // Дождевая вода
```

#### 3. **PBR материалы**
```cpp
/**
 * Создание PBR материала для текстуры
 * @param DiffuseTexture - диффузная текстура
 * @param NormalTexture - нормальная карта
 * @param RoughnessTexture - карта шероховатости
 * @param MetallicTexture - металлическая карта
 * @param AOTexture - карта затенения
 * @param EmissiveTexture - эмиссивная карта
 * @return созданный PBR материал
 */
UMaterial* CreatePBRMaterial(
    UTexture2D* DiffuseTexture,
    UTexture2D* NormalTexture,
    UTexture2D* RoughnessTexture,
    UTexture2D* MetallicTexture,
    UTexture2D* AOTexture,
    UTexture2D* EmissiveTexture
);
```

---

## 🎮 ИНТЕРФЕЙСНЫЕ ТРЕБОВАНИЯ

### 🖥️ Система интерфейсов

#### 1. **Экран входа в игру**
```cpp
/**
 * Экран входа в игру с полями логина и пароля
 * Дизайн в стиле Lineage 2
 */
class ULoginScreenWidget : public UUserWidget
{
public:
    // Поле ввода логина
    UPROPERTY(meta = (BindWidget))
    class UEditableTextBox* LoginTextBox;
    
    // Поле ввода пароля
    UPROPERTY(meta = (BindWidget))
    class UEditableTextBox* PasswordTextBox;
    
    // Кнопка входа
    UPROPERTY(meta = (BindWidget))
    class UButton* LoginButton;
    
    // Кнопка регистрации
    UPROPERTY(meta = (BindWidget))
    class UButton* RegisterButton;
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Вход")
    void OnLoginButtonClicked();
    
    UFUNCTION(BlueprintCallable, Category = "Вход")
    void OnRegisterButtonClicked();
    
    UFUNCTION(BlueprintCallable, Category = "Вход")
    void ValidateCredentials(const FString& Login, const FString& Password);
};
```

#### 2. **Экран выбора персонажей**
```cpp
/**
 * Экран выбора персонажей с кнопкой создания
 * Отображает список существующих персонажей
 */
class UCharacterSelectionWidget : public UUserWidget
{
public:
    // Список персонажей
    UPROPERTY(meta = (BindWidget))
    class UScrollBox* CharacterList;
    
    // Кнопка создания персонажа
    UPROPERTY(meta = (BindWidget))
    class UButton* CreateCharacterButton;
    
    // Кнопка входа в игру
    UPROPERTY(meta = (BindWidget))
    class UButton* EnterGameButton;
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Персонажи")
    void LoadCharacterList();
    
    UFUNCTION(BlueprintCallable, Category = "Персонажи")
    void OnCreateCharacterClicked();
    
    UFUNCTION(BlueprintCallable, Category = "Персонажи")
    void OnEnterGameClicked();
    
    UFUNCTION(BlueprintCallable, Category = "Персонажи")
    void SelectCharacter(int32 CharacterIndex);
};
```

#### 3. **Экран создания персонажа**
```cpp
/**
 * Экран создания персонажа с выбором расы, пола и внешности
 * Поддерживает кастомизацию внешности
 */
class UCharacterCreationWidget : public UUserWidget
{
public:
    // Выбор расы
    UPROPERTY(meta = (BindWidget))
    class UComboBoxString* RaceComboBox;
    
    // Выбор пола
    UPROPERTY(meta = (BindWidget))
    class UComboBoxString* GenderComboBox;
    
    // Слайдер размера груди (только для женских персонажей)
    UPROPERTY(meta = (BindWidget))
    class USlider* BreastSizeSlider;
    
    // Слайдер мускулистости
    UPROPERTY(meta = (BindWidget))
    class USlider* MuscularitySlider;
    
    // Выбор цвета волос
    UPROPERTY(meta = (BindWidget))
    class UColorPicker* HairColorPicker;
    
    // 3D предварительный просмотр
    UPROPERTY(meta = (BindWidget))
    class UImage* CharacterPreview;
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void OnRaceChanged(const FString& SelectedRace);
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void OnGenderChanged(const FString& SelectedGender);
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void OnBreastSizeChanged(float NewValue);
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void OnMuscularityChanged(float NewValue);
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void OnHairColorChanged(FLinearColor NewColor);
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void UpdateCharacterPreview();
    
    UFUNCTION(BlueprintCallable, Category = "Создание")
    void CreateCharacter();
};
```

---

## 🔧 ТЕХНИЧЕСКИЕ СТАНДАРТЫ

### 📝 Стандарты кодирования

#### 1. **Отступы и форматирование**
```cpp
// Используйте 4 пробела для отступов
class ATerrainSystem : public AActor
{
    // Отступ 4 пробела
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Рельеф")
    int32 TerrainSize = 100;
    
    // Отступ 4 пробела
    UFUNCTION(BlueprintCallable, Category = "Рельеф")
    void GenerateTerrain()
    {
        // Отступ 8 пробелов
        for (int32 X = 0; X < TerrainSize; X++)
        {
            // Отступ 12 пробелов
            for (int32 Z = 0; Z < TerrainSize; Z++)
            {
                // Отступ 16 пробелов
                TerrainHeights[X][Z] = GenerateHeight(X, Z);
            }
        }
    }
};
```

#### 2. **Именование функций**
```cpp
// Функции должны быть на русском языке в комментариях
// но имена функций на английском

/**
 * Создает систему рельефа с заданными параметрами
 * @param Size - размер рельефа
 * @param Scale - масштаб рельефа
 */
void CreateTerrainSystem(int32 Size, float Scale);

/**
 * Загружает текстуру из файла
 * @param FilePath - путь к файлу текстуры
 * @return загруженная текстура или nullptr
 */
UTexture2D* LoadTextureFromFile(const FString& FilePath);

/**
 * Применяет текстуру к рельефу на основе высоты
 * @param X - координата X
 * @param Z - координата Z
 * @param Height - высота в этой точке
 */
void ApplyTextureToTerrain(int32 X, int32 Z, float Height);
```

#### 3. **Обработка ошибок**
```cpp
/**
 * Загружает текстуру с обработкой ошибок
 * @param FilePath - путь к файлу
 * @return true если текстура загружена успешно
 */
bool LoadTexture(const FString& FilePath)
{
    // Проверка существования файла
    if (!FPaths::FileExists(FilePath))
    {
        UE_LOG(LogTemp, Error, TEXT("Файл текстуры не найден: %s"), *FilePath);
        return false;
    }
    
    // Загрузка текстуры
    UTexture2D* Texture = LoadObject<UTexture2D>(nullptr, *FilePath);
    if (!Texture)
    {
        UE_LOG(LogTemp, Error, TEXT("Не удалось загрузить текстуру: %s"), *FilePath);
        return false;
    }
    
    UE_LOG(LogTemp, Log, TEXT("Текстура успешно загружена: %s"), *FilePath);
    return true;
}
```

---

## 📊 ТРЕБОВАНИЯ К ПРОИЗВОДИТЕЛЬНОСТИ

### 🚀 Оптимизация

#### 1. **LOD система**
```cpp
/**
 * Система LOD для оптимизации рендеринга
 * Автоматически переключает детализацию в зависимости от расстояния
 */
class ULODSystem : public UObject
{
public:
    // Расстояния переключения LOD
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "LOD")
    float LOD0Distance = 1000.0f;  // Высокая детализация
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "LOD")
    float LOD1Distance = 2000.0f;  // Средняя детализация
    
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "LOD")
    float LOD2Distance = 5000.0f;  // Низкая детализация
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "LOD")
    int32 GetLODLevel(float Distance) const;
    
    UFUNCTION(BlueprintCallable, Category = "LOD")
    void UpdateLOD(AActor* Actor, float Distance);
};
```

#### 2. **Culling система**
```cpp
/**
 * Система отсечения невидимых объектов
 * Улучшает производительность, скрывая объекты за камерой
 */
class UCullingSystem : public UObject
{
public:
    // Радиус отсечения
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Culling")
    float CullingRadius = 10000.0f;
    
    // Функции
    UFUNCTION(BlueprintCallable, Category = "Culling")
    bool IsObjectVisible(AActor* Actor, FVector CameraLocation) const;
    
    UFUNCTION(BlueprintCallable, Category = "Culling")
    void UpdateCulling(AActor* Actor);
};
```

---

## 🎯 ЗАКЛЮЧЕНИЕ

### ✅ Ключевые требования
1. **ВСЕ комментарии на русском языке**
2. **Структурированная архитектура**
3. **PBR материалы для всех текстур**
4. **Система LOD для оптимизации**
5. **Обработка ошибок во всех функциях**

### 🚀 Следующие шаги
1. **Начать с этапа 1** - текстуры земли
2. **Создать базовую архитектуру** систем мира
3. **Интегрировать контент** из деобфусцированного клиента
4. **Оптимизировать производительность** с самого начала

---

*Технические требования созданы: 28 сентября 2024*  
*Статус: Активные требования*  
*Следующий обзор: 1 октября 2024*
